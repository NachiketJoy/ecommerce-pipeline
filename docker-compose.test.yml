version: '3.8'

services:
  backend-test:
    build: ./backend
    ports:
      - "8000:8000"
    environment:
      - NODE_ENV=test

  frontend-test:
    build: ./frontend
    ports:
      - "3000:80"
    depends_on:
      - backend-test

networks:
  default:
    driver: bridge